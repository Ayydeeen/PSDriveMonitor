C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -ExecutionPolicy Bypass -Command "$volumes = 'list volume' | diskpart | Where-Object { $_ -match '^  [^-]' } | Select-Object -skip 1; $DISKState = foreach ($row in $volumes) { if ($row -match 'Failed|At Risk') { $status = 'CRITICAL' }; if ($status -eq 'CRITICAL') { echo $row'is Failed' }}; echo $DISKState"
